var React = require('react');

class Home {
  render() {
    return (
      <div className="home">
        <h1>Home</h1>
      </div>
    );
  }
};

module.exports = React.createClass(Home.prototype);
